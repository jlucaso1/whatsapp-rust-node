/* auto-generated by NAPI-RS */
/* eslint-disable */
export declare class WaBot {
  constructor(dbPath: string, eventCallback: ((err: Error | null, arg: BotEvent) => any))
  start(): Promise<void>
  sendMessage(toJid: string, text: string): Promise<string>
}

export type BotEvent =
  | { type: 'PairingQrCode', code: string, timeout: number }
  | { type: 'Message', info: MessageInfo, textContent?: string }
  | { type: 'Connected' }
  | { type: 'LoggedOut', reason: string }
  | { type: 'Other' }

export interface Jid {
  user: string
  server: string
}

export interface MessageInfo {
  source: MessageSource
}

export interface MessageSource {
  chat: Jid
  sender: Jid
}
